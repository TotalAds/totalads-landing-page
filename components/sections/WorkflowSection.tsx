"use client";

export default function WorkflowSection() {
  return (
    <section id="workflow" className="py-24 bg-gradient-to-br from-slate-800 to-slate-900 relative overflow-hidden">
      <div className="container mx-auto px-4">
        <h2 className="text-3xl lg:text-4xl font-black text-white mb-4 text-center">How it works</h2>
        <p className="text-gray-300 text-center max-w-2xl mx-auto">One URL in. A complete company profile out — vetted, enriched, and scored.</p>
      </div>
    </section>
  );
}

